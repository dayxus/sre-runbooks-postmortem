"""sretriage - evidence-based triage for operational runbooks.

The package lives under ``tools/`` in the repository so the runbook library
stays the top-level artifact. Install it with ``pip install -e .`` (see the
Makefile) or run it with ``PYTHONPATH=tools python3 -m sretriage``.
"""

from __future__ import annotations

__version__ = "0.1.0"

__all__ = ["__version__"]
